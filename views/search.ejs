<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="bg-gray-600">
    <main>
        <nav class="flex p-4 top-0 bg-gray-800 overflow-hidden">
                <h1 class="sm:text-4xl truncate text-xl font-bold text-white">Search Results for <span class="italic text-gray-300"><%= name %></span></h1>
        </nav>
        <div class=" p-4">
            <% for (let i = 0; i < results.length; i++) { %>
                <a href="/result/<%= results[i].id %>" class="flex flex-row w-full items-center border rounded-lg shadow border-gray-700 bg-gray-800 hover:bg-gray-700">
                    <div class="flex justify-center w-1/3 rounded-l-lg items-center bg-white">
                        <img class="object-cover p-4 rounded-t-lg sm:w-48 lg:rounded-none lg:rounded-l-lg" src="<%= results[i].img %>" alt="">
                    </div>
                    <div class="flex flex-col h-full w-2/3 p-4 justify-between leading-normal">
                        <h5 class="mb-1 text-l sm:text-2xl font-bold tracking-tight text-white"><%= results[i].name %></h5>
                        <h6 class="text-xs sm:text-xl line-clamp-3 sm:whitespace-normal font-normal text-gray-400"><%= results[i].description %></h6>
                    </div>
                </a> 
            <% } %>
        </div>
        
    </main>
</body>
</html>